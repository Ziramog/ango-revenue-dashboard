[build]
builder = "nixpacks"
buildCommand = "npm install && cd revenue-frontend && npm install && npm run build && cp -r dist ../dist"

[deploy]
startCommand = "node server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
